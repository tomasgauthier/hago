<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Tombot | Agent Intelligence</title>
    <link
        href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&family=JetBrains+Mono:wght@400;500&display=swap"
        rel="stylesheet">
    <style>
        /* Markdown rendering overrides */
        .message-bot h1,
        .message-bot h2,
        .message-bot h3 {
            font-size: 1rem;
            margin-top: 0.5rem;
        }

        .message-bot p {
            margin-bottom: 0.5rem;
        }

        .message-bot ul,
        .message-bot ol {
            margin-left: 1.25rem;
            margin-bottom: 0.5rem;
        }

        .message-bot code {
            background: #27272a;
            padding: 0.1rem 0.3rem;
            border-radius: 0.2rem;
            font-family: var(--font-mono);
            font-size: 0.8rem;
        }

        .message-bot pre {
            background: #27272a;
            padding: 0.75rem;
            border-radius: 0.4rem;
            overflow-x: auto;
            margin-bottom: 0.5rem;
        }

        .message-bot pre code {
            background: transparent;
            padding: 0;
        }

        :root {
            --bg: #000000;
            --card-bg: #09090b;
            --border: #27272a;
            --border-hover: #3f3f46;
            --primary: #ffffff;
            --secondary: #a1a1aa;
            --accent: #3b82f6;
            --success: #10b981;
            --error: #ef4444;
            --font-main: 'Inter', system-ui, sans-serif;
            --font-mono: 'JetBrains Mono', monospace;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            -webkit-font-smoothing: antialiased;
        }

        body {
            background-color: var(--bg);
            color: var(--primary);
            font-family: var(--font-main);
            line-height: 1.5;
            min-height: 100vh;
        }

        /* Layout */
        .page-wrapper {
            max-width: 1400px;
            margin: 0 auto;
            padding: 2rem;
        }

        header {
            display: flex;
            justify-content: space-between;
            align-items: flex-start;
            margin-bottom: 4rem;
            padding-bottom: 2rem;
            border-bottom: 1px solid var(--border);
        }

        .header-content h1 {
            font-size: 2.5rem;
            font-weight: 700;
            letter-spacing: -0.05em;
            margin-bottom: 0.5rem;
        }

        .header-content p {
            color: var(--secondary);
            font-size: 1rem;
        }

        .nav-actions {
            display: flex;
            gap: 1rem;
        }

        /* Grid System */
        .dashboard-grid {
            display: grid;
            grid-template-columns: repeat(12, 1fr);
            gap: 1.5rem;
        }

        .col-span-12 {
            grid-column: span 12;
        }

        .col-span-8 {
            grid-column: span 8;
        }

        .col-span-6 {
            grid-column: span 6;
        }

        .col-span-4 {
            grid-column: span 4;
        }

        @media (max-width: 1024px) {

            .col-span-8,
            .col-span-6,
            .col-span-4 {
                grid-column: span 12;
            }
        }

        /* Components */
        .section-header {
            margin-bottom: 1.5rem;
            display: flex;
            align-items: center;
            justify-content: space-between;
        }

        .section-header h2 {
            font-size: 0.875rem;
            font-weight: 500;
            text-transform: uppercase;
            letter-spacing: 0.1em;
            color: var(--secondary);
        }

        .card {
            background: var(--card-bg);
            border: 1px solid var(--border);
            border-radius: 0.5rem;
            padding: 1.5rem;
            transition: border-color 0.2s ease, box-shadow 0.2s ease;
        }

        .card:hover {
            border-color: var(--border-hover);
            box-shadow: 0 4px 20px -5px rgba(0, 0, 0, 0.5);
        }

        /* Form Elements */
        .form-group {
            margin-bottom: 1.5rem;
        }

        .form-group:last-child {
            margin-bottom: 0;
        }

        .form-group label {
            display: block;
            font-size: 0.75rem;
            font-weight: 500;
            margin-bottom: 0.5rem;
            color: var(--secondary);
        }

        .input-wrapper {
            position: relative;
        }

        input,
        select,
        textarea {
            width: 100%;
            background: #18181b;
            border: 1px solid var(--border);
            border-radius: 0.375rem;
            padding: 0.625rem 0.875rem;
            color: var(--primary);
            font-size: 0.875rem;
            font-family: var(--font-main);
            transition: all 0.2s ease;
        }

        input:focus,
        select:focus,
        textarea:focus {
            outline: none;
            border-color: var(--accent);
            background: #1d1d21;
        }

        /* Buttons */
        .btn {
            display: inline-flex;
            align-items: center;
            justify-content: center;
            padding: 0.625rem 1.25rem;
            border-radius: 0.375rem;
            font-size: 0.875rem;
            font-weight: 500;
            cursor: pointer;
            transition: all 0.2s ease;
            gap: 0.5rem;
            border: 1px solid transparent;
        }

        .btn-primary {
            background: var(--primary);
            color: var(--bg);
        }

        .btn-primary:hover {
            opacity: 0.9;
        }

        .btn-outline {
            background: transparent;
            border-color: var(--border);
            color: var(--primary);
        }

        .btn-outline:hover {
            background: #18181b;
            border-color: var(--border-hover);
        }

        /* Badges */
        .badge {
            font-size: 0.7rem;
            padding: 0.15rem 0.5rem;
            border-radius: 0.25rem;
            background: #27272a;
            color: #d4d4d8;
            border: 1px solid #3f3f46;
        }

        .badge-status {
            background: rgba(16, 185, 129, 0.1);
            color: var(--success);
            border-color: rgba(16, 185, 129, 0.2);
        }

        /* Skill Grid (Alternative view for providers) */
        .skill-list {
            display: flex;
            flex-direction: column;
            gap: 1rem;
        }

        .skill-item {
            display: flex;
            align-items: center;
            justify-content: space-between;
            padding: 1rem;
            border-radius: 0.375rem;
            background: #141417;
            border: 1px solid var(--border);
        }

        .skill-info {
            display: flex;
            align-items: center;
            gap: 1rem;
        }

        .skill-icon {
            width: 32px;
            height: 32px;
            background: #27272a;
            border-radius: 0.25rem;
            display: flex;
            align-items: center;
            justify-content: center;
            font-weight: 600;
            font-size: 0.75rem;
            color: var(--secondary);
        }

        /* Notification Toast */
        .toast {
            position: fixed;
            bottom: 2rem;
            right: 2rem;
            background: #18181b;
            border: 1px solid var(--border);
            padding: 1rem 1.5rem;
            border-radius: 0.5rem;
            display: flex;
            align-items: center;
            gap: 0.75rem;
            transform: translateX(120%);
            transition: transform 0.3s cubic-bezier(0.4, 0, 0.2, 1);
            z-index: 1000;
            box-shadow: 0 10px 15px -3px rgba(0, 0, 0, 0.3);
        }

        .toast.visible {
            transform: translateX(0);
        }

        /* Auth Overlay */
        #auth-overlay {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: rgba(0, 0, 0, 0.95);
            backdrop-filter: blur(4px);
            display: flex;
            align-items: center;
            justify-content: center;
            z-index: 2000;
            visibility: hidden;
            opacity: 0;
            transition: all 0.2s ease;
        }

        #auth-overlay.visible {
            visibility: visible;
            opacity: 1;
        }

        .auth-card {
            width: 100%;
            max-width: 360px;
            text-align: center;
        }

        .auth-card h2 {
            font-size: 1.5rem;
            margin-bottom: 2rem;
            letter-spacing: -0.025em;
        }

        /* Identity Section Styles */
        .manifesto {
            font-family: var(--font-mono);
            font-size: 0.875rem;
            color: #d4d4d8;
            line-height: 1.7;
            white-space: pre-wrap;
        }

        .principle-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(280px, 1fr));
            gap: 1rem;
            margin-top: 2rem;
        }

        .principle-card {
            padding: 1.25rem;
            border-radius: 0.375rem;
            background: #141417;
            border: 1px solid var(--border);
        }

        .principle-card h4 {
            font-size: 0.8125rem;
            font-weight: 600;
            color: var(--accent);
            margin-bottom: 0.5rem;
        }

        .principle-card p {
            font-size: 0.75rem;
            color: var(--secondary);
        }

        /* Sidebar Styles */
        .chat-sidebar {
            position: fixed;
            top: 0;
            right: 0;
            width: 400px;
            height: 100vh;
            background: var(--card-bg);
            border-left: 1px solid var(--border);
            display: flex;
            flex-direction: column;
            transform: translateX(100%);
            transition: transform 0.3s cubic-bezier(0.4, 0, 0.2, 1);
            z-index: 1500;
            box-shadow: -10px 0 30px rgba(0, 0, 0, 0.5);
        }

        .chat-sidebar.open {
            transform: translateX(0);
        }

        .chat-header {
            padding: 1.5rem;
            border-bottom: 1px solid var(--border);
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .chat-header h3 {
            font-size: 1rem;
            font-weight: 600;
        }

        #chat-messages {
            flex: 1;
            overflow-y: auto;
            padding: 1.5rem;
            display: flex;
            flex-direction: column;
            gap: 1rem;
            scroll-behavior: smooth;
        }

        .message {
            max-width: 85%;
            padding: 0.75rem 1rem;
            border-radius: 0.5rem;
            font-size: 0.875rem;
            line-height: 1.5;
            word-wrap: break-word;
        }

        .message-user {
            align-self: flex-end;
            background: var(--accent);
            color: white;
            border-bottom-right-radius: 0.125rem;
        }

        .message-bot {
            align-self: flex-start;
            background: #18181b;
            border: 1px solid var(--border);
            color: var(--primary);
            border-bottom-left-radius: 0.125rem;
        }

        .chat-input-area {
            padding: 1.5rem;
            border-top: 1px solid var(--border);
            display: flex;
            gap: 0.75rem;
        }

        #chat-input {
            flex: 1;
            resize: none;
            height: 42px;
            min-height: 42px;
            max-height: 150px;
        }

        .chat-toggle-btn {
            position: fixed;
            right: 2rem;
            bottom: 2rem;
            width: 56px;
            height: 56px;
            border-radius: 28px;
            background: var(--accent);
            color: white;
            display: flex;
            align-items: center;
            justify-content: center;
            cursor: pointer;
            box-shadow: 0 4px 12px rgba(59, 130, 246, 0.4);
            z-index: 1400;
            border: none;
            font-size: 1.5rem;
            transition: transform 0.2s ease;
        }

        .chat-toggle-btn:hover {
            transform: scale(1.05);
        }

        /* Scrollbar */
        #chat-messages::-webkit-scrollbar {
            width: 4px;
        }

        #chat-messages::-webkit-scrollbar-thumb {
            background: var(--border);
            border-radius: 2px;
        }
    </style>
    <script src="https://cdn.jsdelivr.net/npm/marked/marked.min.js"></script>
</head>

<body>
    <div class="page-wrapper">
        <header>
            <div class="header-content">
                <h1>Tombot Intelligence</h1>
                <p>Configure agent identity, tools, and system parameters.</p>
            </div>
            <div class="nav-actions">
                <button class="btn btn-outline" onclick="toggleChat()">Chat with Tombot</button>
                <button class="btn btn-outline" onclick="logout()">Logout</button>
            </div>
        </header>

        <main class="dashboard-grid">
            <!-- Left Column: Primary Configs -->
            <div class="col-span-8 dashboard-grid" style="align-content: start;">

                <!-- Status & Actions -->
                <div class="col-span-12 card"
                    style="display: flex; justify-content: space-between; align-items: center;">
                    <div style="display: flex; gap: 2rem; align-items: center;">
                        <div>
                            <span class="badge badge-status">System Operational</span>
                            <span style="font-size: 0.875rem; color: var(--secondary); margin-left: 1rem;"
                                id="last-update">Last synced: Just now</span>
                        </div>
                        <div
                            style="border-left: 1px solid var(--border); padding-left: 2rem; display: flex; gap: 2rem;">
                            <div>
                                <p style="font-size: 0.7rem; color: var(--secondary); text-transform: uppercase;">Total
                                    Cost</p>
                                <p style="font-size: 1.1rem; font-weight: 600; font-family: var(--font-mono);"
                                    id="total-cost">$0.00</p>
                            </div>
                            <div>
                                <p style="font-size: 0.7rem; color: var(--secondary); text-transform: uppercase;">
                                    Compute Tokens</p>
                                <p style="font-size: 1.1rem; font-weight: 600; font-family: var(--font-mono);"
                                    id="total-tokens">0</p>
                            </div>
                        </div>
                    </div>
                    <div>
                        <button class="btn btn-primary" id="save-btn" onclick="saveConfig()">Apply Changes</button>
                    </div>
                </div>

                <!-- LLM Providers -->
                <div class="col-span-12">
                    <div class="section-header">
                        <h2>Providers</h2>
                        <span class="badge" id="provider-count">0</span>
                    </div>
                    <div class="card skill-list" id="providers-list">
                        <!-- Dynamic Content -->
                    </div>
                </div>

                <!-- Identity / Manifesto -->
                <div class="col-span-12">
                    <div class="section-header">
                        <h2>System Soul & Principles</h2>
                        <span class="badge">Safety Mode Active</span>
                    </div>
                    <div class="card">
                        <div class="manifesto" id="bot-persona"></div>
                        <div class="principle-grid" id="bot-principles">
                            <!-- Principles will load here -->
                        </div>
                    </div>
                </div>
            </div>

            <!-- Right Column: Channels & Meta -->
            <div class="col-span-4 dashboard-grid" style="align-content: start;">
                <!-- Channels -->
                <div class="col-span-12">
                    <div class="section-header">
                        <h2>Channels</h2>
                    </div>
                    <div class="card">
                        <div class="form-group">
                            <label>Telegram Token</label>
                            <input type="password" id="telegram-token" placeholder="bot_token_secret">
                        </div>
                        <div class="form-group">
                            <label>Authorized IDs</label>
                            <textarea id="authorized-users" rows="3"
                                placeholder="Enter comma-separated IDs..."></textarea>
                        </div>
                    </div>
                </div>

                <!-- Capabilities -->
                <div class="col-span-12">
                    <div class="section-header">
                        <h2>Core Capabilities</h2>
                    </div>
                    <div class="card">
                        <div class="form-group">
                            <label>Memory System (RAG)</label>
                            <select id="memory-enabled">
                                <option value="true">Active</option>
                                <option value="false">Disabled</option>
                            </select>
                        </div>
                        <div class="form-group">
                            <label>Tool Execution</label>
                            <select id="tools-enabled">
                                <option value="true">Global Enable</option>
                                <option value="false">Global Disable</option>
                            </select>
                        </div>
                        <div class="form-group">
                            <label>Obsidian Vault</label>
                            <input type="text" id="obsidian-path" placeholder="Path to your vault">
                        </div>
                    </div>
                </div>

                <!-- Advanced Features -->
                <div class="col-span-12">
                    <div class="section-header">
                        <h2>Advanced Features</h2>
                        <span class="badge">v2.1</span>
                    </div>
                    <div class="card">
                        <div class="form-group">
                            <label>Browser Automation</label>
                            <select id="browser-enabled">
                                <option value="true">Enabled</option>
                                <option value="false">Disabled</option>
                            </select>
                            <p style="font-size: 0.7rem; color: var(--secondary); margin-top: 0.3rem;">
                                Navigate websites, fill forms, take screenshots (Requires Level 3+)
                            </p>
                        </div>
                        <div class="form-group">
                            <label>File System Access</label>
                            <select id="filesystem-enabled">
                                <option value="true">Enabled</option>
                                <option value="false">Disabled</option>
                            </select>
                            <p style="font-size: 0.7rem; color: var(--secondary); margin-top: 0.3rem;">
                                Read, write, manage files (Requires Level 2+)
                            </p>
                        </div>
                        <div class="form-group">
                            <label>Self-Modification ⚠️</label>
                            <select id="selfmod-enabled">
                                <option value="true">Enabled</option>
                                <option value="false">Disabled</option>
                            </select>
                            <p style="font-size: 0.7rem; color: var(--error); margin-top: 0.3rem;">
                                CAUTION: Agent can modify own config (Requires Level 4)
                            </p>
                        </div>
                    </div>
                </div>

                <!-- Permission System -->
                <div class="col-span-12">
                    <div class="section-header">
                        <h2>Permission System</h2>
                        <span class="badge" id="permission-badge">Level 2</span>
                    </div>
                    <div class="card">
                        <div class="form-group">
                            <label>Maximum Permission Level</label>
                            <select id="permission-level">
                                <option value="1">Level 1 - READ_ONLY (Safest)</option>
                                <option value="2">Level 2 - WRITE_SAFE (Recommended)</option>
                                <option value="3">Level 3 - EXECUTE_SAFE (Automation)</option>
                                <option value="4">Level 4 - PRIVILEGED (Full Access)</option>
                            </select>
                            <p style="font-size: 0.7rem; color: var(--secondary); margin-top: 0.3rem;">
                                Controls what operations the agent can perform
                            </p>
                        </div>
                        <div class="form-group">
                            <label>Allowed Directories (whitelist)</label>
                            <textarea id="allowed-dirs" rows="2" placeholder="./data, ./logs, ./tmp"></textarea>
                            <p style="font-size: 0.7rem; color: var(--secondary); margin-top: 0.3rem;">
                                Comma-separated paths where file operations are allowed
                            </p>
                        </div>
                        <div class="form-group">
                            <label>Denied Directories (blacklist)</label>
                            <textarea id="denied-dirs" rows="2" placeholder="./src, ./node_modules, ./.git"></textarea>
                            <p style="font-size: 0.7rem; color: var(--secondary); margin-top: 0.3rem;">
                                Comma-separated paths that are always forbidden
                            </p>
                        </div>
                        <div class="form-group">
                            <label>Allowed Domains (browser)</label>
                            <textarea id="allowed-domains" rows="2" placeholder="*.github.com, example.com"></textarea>
                            <p style="font-size: 0.7rem; color: var(--secondary); margin-top: 0.3rem;">
                                Leave empty to allow all domains. Use * for wildcards
                            </p>
                        </div>
                        <div class="form-group">
                            <label>Require Approval Level</label>
                            <select id="approval-level">
                                <option value="">No approval required</option>
                                <option value="2">Level 2+</option>
                                <option value="3">Level 3+</option>
                                <option value="4">Level 4 only</option>
                            </select>
                            <p style="font-size: 0.7rem; color: var(--secondary); margin-top: 0.3rem;">
                                Operations at or above this level will require user confirmation
                            </p>
                        </div>
                    </div>
                </div>
            </div>
        </main>
    </div>

    <!-- Chat Sidebar -->
    <div class="chat-sidebar" id="chat-sidebar">
        <div class="chat-header">
            <h3>Tombot Console</h3>
            <button class="btn btn-outline" style="padding: 0.4rem;" onclick="toggleChat()">✕</button>
        </div>
        <div id="chat-messages">
            <div class="message message-bot">Hello! I'm your Tombot assistant. How can I help you today?</div>
        </div>
        <div class="chat-input-area">
            <textarea id="chat-input" placeholder="Type a message..."
                onkeydown="if(event.key === 'Enter' && !event.shiftKey) { event.preventDefault(); sendChatMessage(); }"></textarea>
            <button class="btn btn-primary" onclick="sendChatMessage()" id="send-chat-btn">Send</button>
        </div>
    </div>

    <button class="chat-toggle-btn" onclick="toggleChat()">💬</button>

    <!-- Auth Overlay -->
    <div id="auth-overlay">
        <div class="auth-card">
            <h2>Unlock Intelligence</h2>
            <div class="form-group">
                <input type="password" id="admin-pass" placeholder="Enter Admin Password">
            </div>
            <button class="btn btn-primary" onclick="authenticate()" style="width: 100%;">Authenticate</button>
        </div>
    </div>

    <!-- Notification -->
    <div class="toast" id="toast">
        <div id="toast-icon"></div>
        <span id="toast-text">Configuration updated.</span>
    </div>

    <script>
        let config = null;
        let authPassword = localStorage.getItem('tombot_admin_pass') || '';

        async function init() {
            try {
                const res = await fetch('/api/config', {
                    headers: { 'Authorization': `Bearer ${authPassword}` }
                });

                if (res.status === 401) {
                    document.getElementById('auth-overlay').classList.add('visible');
                    return;
                }

                config = await res.json();
                renderUI();

                // Fetch identity (Soul)
                const idRes = await fetch('/identity');
                const identity = await idRes.json();
                renderIdentity(identity);

                // Fetch Usage
                fetchUsage();

                document.getElementById('auth-overlay').classList.remove('visible');
                document.getElementById('last-update').textContent = 'Last synced: ' + new Date().toLocaleTimeString();
            } catch (err) {
                showToast('API Connection Error', true);
            }
        }

        async function fetchUsage() {
            try {
                const res = await fetch('/api/usage', {
                    headers: { 'Authorization': `Bearer ${authPassword}` }
                });
                if (!res.ok) return;
                const data = await res.json();
                document.getElementById('total-cost').textContent = `$${data.totalCost.toFixed(4)}`;
                document.getElementById('total-tokens').textContent = formatTokens(data.totalTokens);
            } catch (e) { }
        }

        function formatTokens(n) {
            if (n >= 1000000) return (n / 1000000).toFixed(1) + 'M';
            if (n >= 1000) return (n / 1000).toFixed(1) + 'k';
            return n;
        }

        function renderIdentity(id) {
            document.getElementById('bot-persona').textContent = id.persona;
            const principlesDiv = document.getElementById('bot-principles');
            principlesDiv.innerHTML = id.principles.map(p => `
                <div class="principle-card">
                    <h4>${p.name.toUpperCase()}</h4>
                    <p>${p.rule}</p>
                </div>
            `).join('');
        }

        function renderUI() {
            // Providers
            const provList = document.getElementById('providers-list');
            provList.innerHTML = '';
            config.providers.forEach((p, i) => {
                const item = document.createElement('div');
                item.className = 'skill-item';
                item.innerHTML = `
                    <div class="skill-info">
                        <div class="skill-icon">${p.type[0].toUpperCase()}</div>
                        <div>
                            <p style="font-size: 0.875rem; font-weight: 500;">${p.id}</p>
                            <p style="font-size: 0.75rem; color: var(--secondary);">${p.model}</p>
                        </div>
                    </div>
                    <div style="width: 250px;">
                        <input type="password" value="${p.apiKey || p.googleApiKey || ''}"
                               onchange="updateProviderKey(${i}, this.value)"
                               placeholder="API Key">
                    </div>
                `;
                provList.appendChild(item);
            });
            document.getElementById('provider-count').textContent = config.providers.length;

            // Core capabilities
            const tg = config.channels.telegram || {};
            document.getElementById('telegram-token').value = tg.token || '';
            document.getElementById('authorized-users').value = (tg.authorizedUsers || []).join(', ');
            document.getElementById('memory-enabled').value = String(config.memory.enabled);
            document.getElementById('tools-enabled').value = String(config.tools.enabled);
            document.getElementById('obsidian-path').value = config.obsidian.vaultPath || '';

            // Advanced features
            document.getElementById('browser-enabled').value = String(config.browser?.enabled || false);
            document.getElementById('filesystem-enabled').value = String(config.filesystem?.enabled || false);
            document.getElementById('selfmod-enabled').value = String(config.selfModification?.enabled || false);

            // Permission system
            const perms = config.permissions || { maxLevel: 2 };
            document.getElementById('permission-level').value = String(perms.maxLevel || 2);
            document.getElementById('allowed-dirs').value = (perms.allowedDirectories || []).join(', ');
            document.getElementById('denied-dirs').value = (perms.deniedDirectories || []).join(', ');
            document.getElementById('allowed-domains').value = (perms.allowedDomains || []).join(', ');
            document.getElementById('approval-level').value = String(perms.requireApprovalLevel || '');

            // Update permission badge
            updatePermissionBadge(perms.maxLevel || 2);
        }

        function updatePermissionBadge(level) {
            const badge = document.getElementById('permission-badge');
            const labels = {
                1: 'Level 1 - READ_ONLY',
                2: 'Level 2 - WRITE_SAFE',
                3: 'Level 3 - EXECUTE_SAFE',
                4: 'Level 4 - PRIVILEGED'
            };
            badge.textContent = labels[level] || 'Level 2';

            // Update color based on level
            if (level >= 4) {
                badge.style.background = 'rgba(239, 68, 68, 0.1)';
                badge.style.color = 'var(--error)';
                badge.style.borderColor = 'rgba(239, 68, 68, 0.2)';
            } else if (level >= 3) {
                badge.style.background = 'rgba(251, 191, 36, 0.1)';
                badge.style.color = '#fbbf24';
                badge.style.borderColor = 'rgba(251, 191, 36, 0.2)';
            } else {
                badge.style.background = 'rgba(16, 185, 129, 0.1)';
                badge.style.color = 'var(--success)';
                badge.style.borderColor = 'rgba(16, 185, 129, 0.2)';
            }
        }

        // Update permission badge when level changes
        document.addEventListener('DOMContentLoaded', () => {
            const levelSelect = document.getElementById('permission-level');
            if (levelSelect) {
                levelSelect.addEventListener('change', (e) => {
                    updatePermissionBadge(parseInt(e.target.value));
                });
            }
        });

        function updateProviderKey(index, value) {
            const p = config.providers[index];
            if (p.type === 'gemini') p.googleApiKey = value;
            else p.apiKey = value;
        }

        async function saveConfig() {
            const saveBtn = document.getElementById('save-btn');
            saveBtn.disabled = true;
            saveBtn.textContent = 'Saving...';

            try {
                // Core capabilities
                config.channels.telegram.token = document.getElementById('telegram-token').value;
                config.channels.telegram.authorizedUsers = document.getElementById('authorized-users').value
                    .split(',')
                    .map(s => parseInt(s.trim()))
                    .filter(n => !isNaN(n));

                config.memory.enabled = document.getElementById('memory-enabled').value === 'true';
                config.tools.enabled = document.getElementById('tools-enabled').value === 'true';
                config.obsidian.vaultPath = document.getElementById('obsidian-path').value;

                // Advanced features
                if (!config.browser) config.browser = {};
                config.browser.enabled = document.getElementById('browser-enabled').value === 'true';

                if (!config.filesystem) config.filesystem = {};
                config.filesystem.enabled = document.getElementById('filesystem-enabled').value === 'true';

                if (!config.selfModification) config.selfModification = {};
                config.selfModification.enabled = document.getElementById('selfmod-enabled').value === 'true';

                // Permission system
                if (!config.permissions) config.permissions = {};
                config.permissions.maxLevel = parseInt(document.getElementById('permission-level').value);

                // Parse allowed directories
                const allowedDirsText = document.getElementById('allowed-dirs').value.trim();
                config.permissions.allowedDirectories = allowedDirsText
                    ? allowedDirsText.split(',').map(s => s.trim()).filter(s => s)
                    : [];

                // Parse denied directories
                const deniedDirsText = document.getElementById('denied-dirs').value.trim();
                config.permissions.deniedDirectories = deniedDirsText
                    ? deniedDirsText.split(',').map(s => s.trim()).filter(s => s)
                    : [];

                // Parse allowed domains
                const allowedDomainsText = document.getElementById('allowed-domains').value.trim();
                config.permissions.allowedDomains = allowedDomainsText
                    ? allowedDomainsText.split(',').map(s => s.trim()).filter(s => s)
                    : [];

                // Parse approval level
                const approvalLevelVal = document.getElementById('approval-level').value;
                if (approvalLevelVal) {
                    config.permissions.requireApprovalLevel = parseInt(approvalLevelVal);
                } else {
                    delete config.permissions.requireApprovalLevel;
                }

                const res = await fetch('/api/config', {
                    method: 'POST',
                    headers: {
                        'Content-Type': 'application/json',
                        'Authorization': `Bearer ${authPassword}`
                    },
                    body: JSON.stringify(config)
                });

                if (!res.ok) throw new Error('Update Rejected');

                showToast('System Updated Successfully');
                document.getElementById('last-update').textContent = 'Last synced: ' + new Date().toLocaleTimeString();
            } catch (err) {
                showToast(err.message, true);
            } finally {
                saveBtn.disabled = false;
                saveBtn.textContent = 'Apply Changes';
            }
        }

        function authenticate() {
            authPassword = document.getElementById('admin-pass').value;
            localStorage.setItem('tombot_admin_pass', authPassword);
            init();
        }

        function logout() {
            localStorage.removeItem('tombot_admin_pass');
            location.reload();
        }

        function showToast(text, isError = false) {
            const toast = document.getElementById('toast');
            const icon = document.getElementById('toast-icon');
            document.getElementById('toast-text').textContent = text;
            toast.style.borderColor = isError ? 'var(--error)' : 'var(--success)';
            toast.classList.add('visible');
            setTimeout(() => toast.classList.remove('visible'), 3000);
        }

        /* Chat Logic */
        const chatSidebar = document.getElementById('chat-sidebar');
        const chatMessages = document.getElementById('chat-messages');
        const chatInput = document.getElementById('chat-input');
        const sessionKey = 'admin:dashboard'; // Persistent session key

        function toggleChat() {
            chatSidebar.classList.toggle('open');
            if (chatSidebar.classList.contains('open')) {
                chatInput.focus();
                // Load history if empty
                if (chatMessages.children.length <= 1) {
                    fetchChatHistory();
                }
            }
        }

        async function fetchChatHistory() {
            try {
                const res = await fetch(`/api/messages?sessionKey=${sessionKey}`, {
                    headers: { 'Authorization': `Bearer ${authPassword}` }
                });
                if (!res.ok) return;
                const history = await res.json();

                // Clear initial placeholder except the very first greeting if wanted
                chatMessages.innerHTML = '';

                history.forEach(msg => {
                    addMessage(msg.content, msg.role === 'user' ? 'user' : 'bot');
                });
            } catch (e) {
                console.error('Failed to fetch history', e);
            }
        }

        async function sendChatMessage() {
            const text = chatInput.value.trim();
            if (!text) return;

            // Add user message to UI
            addMessage(text, 'user');
            chatInput.value = '';

            // Create placeholder for bot response
            const botMsgDiv = addMessage('', 'bot');
            const sendBtn = document.getElementById('send-chat-btn');
            sendBtn.disabled = true;

            try {
                const response = await fetch('/api/chat', {
                    method: 'POST',
                    headers: {
                        'Content-Type': 'application/json',
                        'Authorization': `Bearer ${authPassword}`
                    },
                    body: JSON.stringify({ sessionKey, text })
                });

                if (!response.ok) {
                    const err = await response.json();
                    botMsgDiv.innerText = 'Error: ' + (err.error || response.statusText);
                    return;
                }

                // Handle streaming
                const reader = response.body.getReader();
                const decoder = new TextDecoder();
                let fullResponse = '';

                while (true) {
                    const { done, value } = await reader.read();
                    if (done) break;

                    const chunk = decoder.decode(value, { stream: true });
                    fullResponse += chunk;

                    // Render markdown as we go
                    botMsgDiv.innerHTML = marked.parse(fullResponse);
                    chatMessages.scrollTop = chatMessages.scrollHeight;
                }
            } catch (err) {
                botMsgDiv.innerText = 'Connection Error: ' + err.message;
            } finally {
                sendBtn.disabled = false;
            }
        }

        function addMessage(text, role) {
            const div = document.createElement('div');
            div.className = `message message-${role}`;
            if (role === 'bot') {
                div.innerHTML = marked.parse(text || '');
            } else {
                div.textContent = text;
            }
            chatMessages.appendChild(div);
            chatMessages.scrollTop = chatMessages.scrollHeight;
            return div;
        }

        init();
    </script>
</body>

</html>